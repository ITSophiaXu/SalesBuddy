# Motive · 海外汽车销售 Cowork

代码包版本：1.8.1。本版修正对话正文可读性、标题操作区对齐、复制与保存按钮以及重复气泡。左侧选择销售、市场或区域经理，主导航为“今日工作、工作台、AI 来信、生态”。首页直接提供单一 Cowork 对话区，处理问答、话术与客户沟通记录；具体任务进入工作台，中间显示文档、海报与版本，右侧延续同一段对话。客户画像、车源与方案、营销活动保留在工作台二级入口。

工作台顶部可以切换成果和查看版本。点击“编辑”直接修改正文或海报；保存后生成新版本。右侧对话始终参考中间选中的成果，未保存的修改会先提示保存。任务负责人、下一步和完成状态在顶部“任务信息”中。

销售演示：在首页输入“为 Sarah 比较 Tesla Model Y 和 Toyota RAV4 Hybrid，侧重家庭空间、购车预算和补能便利，生成中文车型对比方案。”进入工作台后，再说“请调整当前车型对比，把用车成本放在最前面，其它重点保留。”可通过“审核与导出 → 导出客户对比页”得到独立 HTML 文件。示例文件位于 artifacts/Motive-Vehicle-Comparison-Demo.html。

角色场景与逐步演示见 [三角色 UX 方案](Motive-三角色Cowork-UX设计-v3.md)。

## 本次代码包的真实状态

SDK 接入、配置读取、CLI 定位、异常恢复和调用检查已实现。生成本包时，当前环境的 SDK 下载被网络限制及自动审批服务错误 `model_not_supported` 阻止，**尚未安装 SDK，也未通过真实大模型调用验证**。包内 `RELEASE.json` 保存本次状态；自动测试通过不能代替此验证。

这是源码包，包含安装入口；不包含 Node.js、下载好的依赖、账户身份或客户数据。需要在获准联网的运行环境完成下述安装和账户登录。无需把密码、GitHub Token 或 SSH 私钥发到聊天中。

## Windows 本机

1. 安装完整的 [Node.js 22 或更新版本](https://nodejs.org/)，包括 npm。仅有 Codex 自带 Node 运行文件不够。
2. 解压代码包，双击 **安装并连接Copilot.cmd**。
3. 安装程序会在项目内安装 `@github/copilot-sdk` 和 `@github/copilot`，生成 `.env`；已有配置保持不变。首次安装解析 npm 当前版本，并将实际版本锁定到 `package-lock.json`。
4. 如尚未登录，会打开 Copilot CLI。使用有 Copilot 权限的账户，按提示登录，或输入 `/login`；登录完成后输入 `/exit` 返回检查。
5. 程序依次验证普通问答、短话术、销售对话整理画像、任务识别、方案与海报。只有五项都成功才显示已通过。检查最多进行五次真实模型调用，仅使用内置虚构资料，可能计入该账户用量。结果在 `runtime/copilot-check.json`。
6. 通过后双击 **启动Motive.cmd**，打开 <http://127.0.0.1:4173/#home>。

若已有项目依赖或希望分别操作，在项目目录执行：

```sh
npm install
npm run login:copilot
npm run verify:copilot
npm start
```

安装使用当前 npm 软件包源，不更改代理、系统审批或全局设置。若安装失败，请由运行环境管理员解决网络或软件包源访问问题。若显示订阅或权限问题，需要为运行服务的账户开通 Copilot。浏览器或 VS Code 中登录 GitHub 不等于服务账户已经登录 Copilot CLI。

## Azure Linux VM

本包包含 Docker Compose 配置，步骤见 `DEPLOYMENT.md`。重点是：先安装依赖 → 使用容器服务账户登录 Copilot → 执行真实调用检查 → 启动服务。依赖版本在部署时锁定；Copilot 身份保存于单独的持久卷。

本包没有执行远程部署，也没有修改 VM 上的其他服务。

## 可选模型设置

服务启动和连接检查都会读取项目根目录 `.env`，宿主环境变量优先。默认使用 Copilot 可用的默认模型。要固定模型，在 `.env` 中填写账户实际可用的 `COPILOT_MODEL`，然后重启服务。

优先使用运行服务账户的 Copilot CLI 登录。若管理员提供身份文件，可配置 `COPILOT_GITHUB_TOKEN_FILE`。身份文件必须保存在服务器的私有位置，不放进代码包；应用不会把身份值返回给浏览器。

## 核心体验流程

- **销售沟通与画像**：客户与画像 → 选择客户 → 记录沟通，描述实际客户反馈，例如“沟通记录：需要放两个儿童座椅。太太会一起试驾后决定。计划月底购车。” → 核对并记入画像 → 勾选、编辑并确认。每条保留原话、来源、记录时间与原对话；可补充或替换，销售判断单独标为待核实。之后继续要求“依据更新后的画像准备选车方案”。
- **简单问题与话术**：Cowork 输入“为什么客户试驾后不回复？”，再说“帮 Sarah 写一句自然的英文跟进话术”。直接在对话中查看、修改、复制。
- **销售线索转化**：客户 → 新增销售线索 → 填电话及已有信息 → 保存并制定转化计划。在原任务添加下一步与负责人，电话沟通后记录真实客户反馈，再调整方案。
- **车源与方案**：导航直接进入 → 选择市场 → 为客户匹配，或在车型卡点击匹配客户 → 结合画像生成方案；费用试算仅用于假设条件，不是正式报价。
- **营销活动**：导航直接进入 → 创建活动 → 分群名单 / 活动包 / 设计海报。也可从 AI 协作发起，资料范围选择“美国 · 门店资料”，输入“制定家庭 SUV 营销方案，包含客群、邀请内容、海报和执行安排”。在同一任务检查方案、编辑海报，工作台找回任务及待办。

客户与会话仍保存在本地浏览器工作区；CSV 是导入快照。真实 CRM、后台全天候定时执行、客户消息发送、订单与收款接口尚未接入。
